{
  "name": "aiweb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "uv pip install --system mkdocs mkdocs-material && python3 -m mkdocs build",
    "dev": "python3 -m mkdocs serve"
  }
}
