# AI 应用部署运维

AI 应用上线后，运维关注点不仅是服务可用，还包括模型质量、推理成本、延迟、数据安全、提示词版本、检索索引和工具调用风险。

## 运维对象

| 对象 | 需要监控的内容 |
|------|----------------|
| 模型服务 | QPS、并发、首 token 延迟、总延迟、错误率、超时、成本 |
| RAG 链路 | 文档更新、索引构建、召回质量、引用覆盖率、空召回率 |
| Agent 工具 | 调用次数、参数错误、权限拒绝、外部 API 失败、重试次数 |
| Prompt 版本 | 模板变更、输出格式错误、质量回归和用户反馈 |
| 数据安全 | 敏感信息、越权访问、日志脱敏和审计记录 |

## 上线前清单

- 是否有评估集覆盖核心任务和高风险场景。
- 是否记录 Prompt、模型、参数、检索结果和工具调用版本。
- 是否设置超时、重试、限流、熔断和降级回答。
- 是否对用户输入和模型输出做必要的安全过滤。
- 是否能追踪一次回答从输入到最终输出的完整链路。
- 是否有成本预算、告警阈值和异常流量处理策略。

## 日志与追踪

生产日志不应只保存最终回答。建议至少记录：

- 请求 ID、用户或租户标识、入口和时间。
- 模型名称、参数、上下文长度、token 用量和耗时。
- 检索查询、召回文档 ID、重排序分数和引用来源。
- 工具调用名称、参数摘要、状态码、耗时和失败原因。
- 输出解析状态、拒答原因、用户反馈和人工审核结果。

涉及隐私或商业敏感内容时，应保存摘要、哈希或脱敏字段，而不是完整明文。

## 降级策略

| 故障 | 可选降级 |
|------|----------|
| 模型 API 超时 | 切换备用模型、缩短上下文、返回可解释的稍后重试 |
| 向量库不可用 | 使用关键词检索、缓存答案或静态 FAQ |
| 工具 API 失败 | 停止执行动作，返回失败原因和人工处理入口 |
| 输出格式错误 | 自动重试结构化修复，超过阈值进入人工队列 |
| 成本异常 | 降低并发、切小模型、关闭低优先级批任务 |

### 多级降级链路

生产系统应设计分层降级，避免单点故障直接暴露给用户：

```
主模型（高能力）
  → 超时/错误 → 备用模型（同类不同厂商）
  → 仍失败 → 语义缓存命中（历史相似问答）
  → 未命中 → 小模型快速兜底回复
  → 仍失败 → 静态 FAQ / 转人工
```

设计要点：

- **熔断器**：某下游连续失败达到阈值后短路，避免级联雪崩。
- **超时分级**：首 token 超时（影响体感）与总时长超时（影响完整性）分开设置。
- **优雅降级回复**：降级时告知用户"当前服务降级，结果可能不完整"，而非静默返回低质量答案。
- **可观测**：每次降级都记录触发原因，用于事后归因和容量规划。

## 质量闭环

1. 收集线上失败案例和用户负反馈。
2. 标注失败类型：检索失败、幻觉、格式错误、工具错误、权限错误或需求不清。
3. 将高频问题加入离线评估集。
4. 修改 Prompt、索引、工具 schema、模型路由或业务规则。
5. 回归测试通过后灰度发布，并继续监控关键指标。

## 相关文档

- [LLM 应用架构](/进阶学习/LLM应用架构/)
- [模型评估与基准](/进阶学习/模型评估与基准/)
- [开源模型部署选型](/模型专区/开源模型部署选型/)
- [Agent 评估与可观测性](/AIAgent实践/Agent评估与可观测性/)

---

## 精选资源

> 该区块由采集脚本根据资源库自动重建，只保留当前专题最相关的精选链接；正文教程不会被自动覆盖。

<!-- RESOURCES_START -->

- **[配置和运行生成式AI应用|云架构中心](https://docs.cloud.google.com/architecture/deploy-operate-generative-ai-applications?hl=zh-cn)**
  - 来源：`docs.cloud.google.com` · 质量分：13 · 首次采集：2026-07-02 · 信息源：`tavily` · 已验证：2026-07-02
  - # 部署和运行生成式 AI 应用 使用集合让一切井井有条 根据您的偏好保存内容并对其进行分类。. 生成式 AI 引入了一种不同于预测性 AI 的全新 AI 应用构建和运行方式。若要构建生成式 AI 应用，您必须从各种架构和规模中进行选择、整理数据、设计最佳提示、针对特定任务调整模型，并根据真实数据确定模型输出。. 本文档介绍了如何调整 DevOps 和 MLOps 流程，以基于现有基础模型开发、部署和运行生成式 AI 应用。如需了解如何...

- **[AI数据分析工具驱动监控运维：构建智能、高效、预测性体系_git_Wang_GEO-智能体开发者社区](https://adg.csdn.net/696f2dde437a6b4033699834.html)**
  - 来源：`adg.csdn.net` · 质量分：9 · 首次采集：2026-07-02 · 信息源：`tavily` · 已验证：2026-07-02
  - **人工****智能数据分析****（****AI****数据分析****）**技术的崛起，为突破这些瓶颈提供了革命性的解决方案。**ai数据分析工具**和**人工智能数据分析软件**正以地下室的方式，将M&O从宇航员的“响应式模式”通过对来自日志、指标、订单追踪、事件等海量运维数据的深度挖掘和智能分析，AI能够识别早期异常、准确预测潜在故障、自动定位路径，甚至提出优化建议，从而显着提升保障运维效率，业务永续。 本文将深入探讨如何运用*...

- **[辅助运维监控 - AI 辅助软件工程：实践与案例解析](https://aise.phodal.com/gen-monitor.html)**
  - 来源：`aise.phodal.com` · 质量分：8 · 首次采集：2026-07-02 · 信息源：`tavily` · 已验证：2026-07-02
  - # AI 辅助软件工程：实践与案例解析. # AI 辅助软件工程：AI 辅助运维监控. 这些能力的引入，正推动AIOps向更主动、更智能，并最终向更自主的运维模式演进 。生成式AI不仅仅是为AIOps增加了新的功能模块，更深层次地， 它正在重塑人与AIOps系统之间的交互模式。AIOps平台正从单纯的分析工具转变为更像一个能够与运维人员协同工作的“智能伙伴”。 市场上主流AIOps解决方案纷纷推出“Copilot”或“Assistant...

- **[OpsAny - AI原生运维管理平台](https://opsany.com/)**
  - 来源：`opsany.com` · 质量分：7 · 首次采集：2026-07-02 · 信息源：`tavily` · 已验证：2026-07-02
  - 智能运维平台[](https://www.opsany.com/)。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。 。

<!-- RESOURCES_END -->

*资源区块更新时间：2026-07-02 11:56:59*
